apiVersion: delivery.ocm.software/v1alpha1
kind: Component
metadata:
  name: podinfo-component
spec:
  component: github.com/open-component-model/podinfo
  interval: 10m
  repositoryRef:
    name: podinfo-repository
    namespace: default
  semver: v6.0.0
